const { sumNumber } = require("../src/index");

// test('should be able return true if the function exists ', () => {
//     expect(sumNumber()).toBe(true);
// });

// test('should be able return true if number is 1', () => {
//     const number = 1;
//     expect(sumNumber(number)).toBe(true);
// });

test('should be able return  the sum of every number from 1 to n', () => {
    const number = 0;
    const count = 0;
    expect(sumNumber(number)).toBe(count);
});